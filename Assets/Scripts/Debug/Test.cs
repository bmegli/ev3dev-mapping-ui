using UnityEngine;
using System.Collections;
using CircularBuffer;
using System.IO;

public class Test : MonoBehaviour {

	// Use this for initialization
	void Start () {
		/*
		BinaryWriter bw=new BinaryWriter(File.Open("plytest.ply", FileMode.OpenOrCreate));
		PLYPolygonFileFormat.EmitHeader(bw, 100, "genarated with ev3dev-mapping");
		bw.Close();
		*/
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
